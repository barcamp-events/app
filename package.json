{
  "name": "barcamp-events",
  "version": "1.0.0",
  "description": "The app for barcamp.events",
  "main": "index.js",
  "scripts": {
    "develop:ios": "npx cap open ios",
    "develop:android": "npx cap open android",
    "develop:electron": "npx cap open electron",
    "build": "NODE_ENV=production stencil build --prod",
    "build.prod": "stencil build --prod && npx cap copy",
    "start": "stencil build --dev --watch --serve",
    "start.prod": "stencil build --prod --watch --serve",
    "test": "jest",
    "test.watch": "jest --watch"
  },
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "jest": "^23.4.2",
    "workbox-build": "4.3.1",
    "rollup-plugin-replace": "^2.2.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^1.1.1",
    "@capacitor/cli": "^1.1.1",
    "@capacitor/core": "^1.1.1",
    "@capacitor/ios": "^1.1.1",
    "@rawmodel/core": "^3.0.0-alpha7",
    "@stellar-design/core": "^1.2.9",
    "@stellar-design/device": "^0.1.2",
    "@stencil/core": "^1.3.1",
    "@stencil/router": "^1.0.1",
    "@stencil/state-tunnel": "^1.0.1",
    "firebase": "^5.5.3",
    "rollup-plugin-dotenv": "^0.2.0"
  },
  "jest": {
    "preset": "@stencil/core/testing"
  }
}
