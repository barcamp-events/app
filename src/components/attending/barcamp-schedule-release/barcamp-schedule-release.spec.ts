import { BarcampScheduleRelease } from "./barcamp-schedule-release";

describe("barcamp-schedule-release", () => {
  it("builds", () => {
    expect(new BarcampScheduleRelease()).toBeTruthy();
  });
});
