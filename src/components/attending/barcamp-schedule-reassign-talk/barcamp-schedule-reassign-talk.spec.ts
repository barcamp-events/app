import { BarcampScheduleReassignTalk } from "./barcamp-schedule-reassign-talk";

describe("barcamp-schedule-reassign-talk", () => {
  it("builds", () => {
    expect(new BarcampScheduleReassignTalk()).toBeTruthy();
  });
});
