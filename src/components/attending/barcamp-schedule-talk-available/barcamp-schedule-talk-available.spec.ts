import { BarcampScheduleTalkAvailable } from './barcamp-schedule-talk-available';

describe('barcamp-schedule-talk-available', () => {
  it('builds', () => {
    expect(new BarcampScheduleTalkAvailable()).toBeTruthy();
  });
});
