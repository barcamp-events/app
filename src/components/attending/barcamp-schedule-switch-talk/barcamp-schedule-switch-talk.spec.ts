import { BarcampScheduleSwitchTalk } from './barcamp-schedule-switch-talk';

describe('barcamp-schedule-switch-talk', () => {
  it('builds', () => {
    expect(new BarcampScheduleSwitchTalk()).toBeTruthy();
  });
});
