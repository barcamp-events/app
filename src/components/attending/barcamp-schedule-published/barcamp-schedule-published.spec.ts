import { BarcampSchedulePublished } from './barcamp-schedule-published';

describe('barcamp-schedule-published', () => {
  it('builds', () => {
    expect(new BarcampSchedulePublished()).toBeTruthy();
  });
});
