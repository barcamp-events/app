import { BarcampDefaultMarketing } from '../barcamp-default-marketing';

describe('barcamp-default-marketing', () => {
  it('builds', () => {
    expect(new BarcampDefaultMarketing()).toBeTruthy();
  });
});
