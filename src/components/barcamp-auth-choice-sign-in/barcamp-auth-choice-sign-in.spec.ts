import { BarcampAuthChoiceSignIn } from './barcamp-auth-choice-sign-in';

describe('barcamp-auth-choice-sign-in', () => {
  it('builds', () => {
    expect(new BarcampAuthChoiceSignIn()).toBeTruthy();
  });
});
