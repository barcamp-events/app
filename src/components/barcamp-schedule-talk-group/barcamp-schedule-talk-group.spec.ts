import { BarcampScheduleTalkGroup } from './barcamp-schedule-talk-group';

describe('barcamp-schedule-talk-group', () => {
  it('builds', () => {
    expect(new BarcampScheduleTalkGroup()).toBeTruthy();
  });
});
