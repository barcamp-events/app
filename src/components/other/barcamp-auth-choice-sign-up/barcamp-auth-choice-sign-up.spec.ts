import { BarcampAuthChoiceSignUp } from './barcamp-auth-choice-sign-up';

describe('barcamp-auth-choice-sign-up', () => {
  it('builds', () => {
    expect(new BarcampAuthChoiceSignUp()).toBeTruthy();
  });
});
