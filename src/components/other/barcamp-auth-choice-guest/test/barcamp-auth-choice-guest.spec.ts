import { BarcampAuthChoiceGuest } from '../barcamp-auth-choice-guest';

describe('barcamp-auth-choice-guest', () => {
  it('builds', () => {
    expect(new BarcampAuthChoiceGuest()).toBeTruthy();
  });
});
