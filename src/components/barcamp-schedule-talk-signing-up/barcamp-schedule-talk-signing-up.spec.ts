import { BarcampScheduleTalkSigningUp } from './barcamp-schedule-talk-signing-up';

describe('barcamp-schedule-talk-signing-up', () => {
  it('builds', () => {
    expect(new BarcampScheduleTalkSigningUp()).toBeTruthy();
  });
});
